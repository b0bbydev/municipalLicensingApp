<h2 class="text-center">Dropdown Manager</h2>
<div class="content">
  <!-- Table -->
  <div class="row">
    <div class="col-12">
      <table
        class="sortable table table-sm table-bordered table-striped table-hover"
      >
        <thead>
          <tr>
            <th>Form ID</th>
            <th>Form Name</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {{#each data}}
            <tr>
              <td><a
                  href="/dropdownManager/form/{{this.formID}}"
                  id="ownerLink"
                >{{this.formID}}</a></td>
              <td>{{this.formName}}</td>
              <td><a href="#" class="btn btn-sm btn-secondary">Actions</a></td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>

  <!-- check for error messages in the session -->
  {{#if message}}
    <div class="alert alert-danger">
      {{message}}
    </div>
  {{/if}}

  <!-- Add new dropdown value -->
  {{!-- <h4>Add A New Value</h4>
   <form method="POST">
      <div class="row">
         <div class="col-sm-2">
            <label for="Dropdown Value" class="form-label"></label>
            <input name="value" class="form-control" placeholder="New Value..." type="text" pattern="[^()/><\][\\\x22,;|]+" title="No special characters!" required>
         </div>
      </div>
      <button class="btn btn-primary" type="submit">Submit</button>
   </form>
   
   <div class="row">
      <h4>Test Dropdown</h4>
      <div class="row">
         <div class="col-sm-2">
            <select name="dropdown" class="form-select" id="dropdown">
               <option value="default" disabled selected>select a value...</option>
               {{#each data}}
               <option value="{{this.dropdownID}}" {{#if this.isDisabled}} disabled {{/if}}>{{this.value}}</option>
               {{/each}}
            </select>
         </div>
      </div>
   </div> --}}
</div>